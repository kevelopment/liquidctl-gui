/**
 * All speed options that can be set by liquidctl.
 *
 * @export
 * @enum {number}
 */
export enum SPEEDS {
  SLOWEST = "slowest",
  SLOWER = "slower",
  NORMAL = "normal",
  FASTER = "faster",
  FASTEST = "fastest",
}
